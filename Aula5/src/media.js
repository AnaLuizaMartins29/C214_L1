function calcularMedia(np1, np2) {
    return ((np1+np2) / 2).toFixed(2); // Calcula o a media dass notas com duas casas decimais
}

module.exports = { calcularMedia }; // Exporta a função calcularMedia